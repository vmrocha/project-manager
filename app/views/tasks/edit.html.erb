<h1>Editing task</h1>

<%= render "form", task: @task %>

<br>

<div>
  <%= link_to "Show this task", project_task_path(@task.project_id, @task.id) %> |
  <%= link_to "Back to project", project_path(@task.project) %>
</div>
